@using Business_Manager.Helpers
@{
    string itemName = "ItemSegmentValue";
    string itemNameText = "節段代碼值管理";
}

@Html.Resource("css",
    @<style>
    </style>
)

<div class="modal fade" id="modalQuery@(itemName)">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">搜尋</h5>
                <button class="close" data-dismiss="modal" type="button">
                    <span aria-hidden="true"><i class="fas fa-times"></i></span>
                </button>
            </div>
            <div class="modal-body">
                <form class="container" autocomplete="off" id="queryForm@(itemName)">
                    <fieldset>
                        <div class="form-group row">
                            <label class="col-12 col-form-label" for="txtSegmentValueNoQ@(itemName)">代碼</label>
                            <div class="col-12">
                                <input type="text" class="form-control" id="txtSegmentValueNoQ@(itemName)" name="txtSegmentValueNoQ@(itemName)" placeholder="代碼" />
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-12 col-form-label">生效日<input type="checkbox" id="cbxEffectiveDateQ@(itemName)" /></label>
                            <div class="col-12">
                                <input type="text" class="form-control" id="txtEffectiveDateQ@(itemName)" name="txtEffectiveDateQ@(itemName)"
                                       data-msg-extraRegex="請輸入正確的日期格式" data-rule-extraRegex="([12]\d{3}\-(0[1-9]|1[0-2])\-(0[1-9]|[12]\d|3[01]))"
                                       readonly />
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-12 col-form-label">失效日<input type="checkbox" id="cbxInactiveDateQ@(itemName)" /></label>
                            <div class="col-12">
                                <input type="text" class="form-control" id="txtInactiveDateQ@(itemName)" name="txtInactiveDateQ@(itemName)"
                                       data-msg-extraRegex="請輸入正確的日期格式" data-rule-extraRegex="([12]\d{3}\-(0[1-9]|1[0-2])\-(0[1-9]|[12]\d|3[01]))"
                                       readonly />
                            </div>
                        </div>
                    </fieldset>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" onclick="Query@(itemName)()" style="margin: auto;"><i class="fal fa-wrench"></i>套用</button>
            </div>
        </div>
    </div>
</div>

<div class="row" id="listData@(itemName)">
    <div class="col-12">
        <div class="card card-shadow mb-4">
            <div class="card-body">
                <div class="row mb-3">
                    <div class="col-6 col-sm-6 col-md-6">
                        <a class="btn btn-primary" data-backdrop="static" data-keyboard="false" data-toggle="modal" data-target="#modalQuery@(itemName)" href="javascript:void(0);">
                        <i class="fal fa-search"></i>搜尋</a>
                    </div>
                    <div class="col-6 col-sm-6 col-md-6 text-right">
                        <a class="btn btn-info auth-detail" href="javascript:Edit@(itemName)();"><i class="fas fa-plus"></i>新增</a>
                    </div>
                </div>
                <div class="row">
                    <div class="col-12">
                        <div class="table-custom">
                            <table class="table table-bordered table-striped table-sticky" id="tblData@(itemName)">
                                <thead>
                                    <tr>
                                        <th scope="col" style="min-width: 75px;">#</th>
                                        <th scope="col" style="min-width: 200px;">代碼</th>
                                        <th scope="col" style="min-width: 200px;">代碼值</th>
                                        <th scope="col" style="min-width: 200px;">狀態</th>
                                        <th class="text-center" scope="col" style="min-width: 125px;">操作</th>
                                    </tr>
                                </thead>
                                <tbody></tbody>
                            </table>
                        </div>
                        <div class="pagination" id="page@(itemName)"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row" id="editData@(itemName)" style="display: none;">
    <div class="col-12">
        <div class="card card-shadow mb-4">
            <div class="card-body">
                <form class="container custom-form-container" autocomplete="off" id="editForm@(itemName)">
                    <fieldset>
                        <input type="hidden" id="SegmentValueId" name="SegmentValueId" value="-1" />
                        <div class="form-group row">
                            <label class="col-12 col-md-3 col-lg-2 col-form-label" for="txtSegmentValueNo@(itemName)">代碼 <span class="text-danger">*</span></label>
                            <div class="col-12 col-md-9 col-lg-10">
                                <input type="text" class="form-control" id="txtSegmentValueNo@(itemName)" name="txtSegmentValueNo@(itemName)" placeholder="請輸入代碼                    "
                                       data-msg-required="請輸入代碼" data-rule-required="true" onkeyup="this.value=Check(this.value.toUpperCase().replace(/[, ]/g,''))"
                                       data-msg-maxlength="必須少於 20 個字元" maxlength="20" />
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-12 col-md-3 col-lg-2 col-form-label" for="txtSegmentValue@(itemName)">代碼名稱 <span class="text-danger">*</span></label>
                            <div class="col-12 col-md-9 col-lg-10">
                                <input type="text" class="form-control" id="txtSegmentValue@(itemName)" name="txtSegmentValue@(itemName)" placeholder="請輸入代碼名稱"
                                       data-msg-required="請輸入代碼名稱" data-rule-required="true"
                                       data-msg-maxlength="必須少於 200 個字元" maxlength="200" />
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-12 col-md-3 col-lg-2 col-form-label" for="txtSegmentValueDesc@(itemName)">代碼說明 <span class="text-danger">*</span></label>
                            <div class="col-12 col-md-9 col-lgㄑ10">
                                <input type="text" class="form-control" id="txtSegmentValueDesc@(itemName)" name="txtSegmentValueDesc@(itemName)" placeholder="請輸入代碼說明"
                                       data-msg-required="請輸入代碼說明" data-rule-required="true"
                                       data-msg-maxlength="必須少於 200 個字元" maxlength="200" />
                            </div>
                        </div>
                        <div class="form-group row">
                            <div class="col-lg-12 col-md-12">
                                <div class="form-group row">
                                    <label class="col-12 col-md-3 col-lg-2 col-form-label" for="txtEffectiveDate@(itemName)">生效日</label>
                                    <div class="col-12 col-md-9 col-lg-4">
                                        <div class="btn-group"  style="width:100%;">
                                            <input type="text" class="form-control" id="txtEffectiveDate@(itemName)" name="txtEffectiveDate@(itemName)"
                                                   data-msg-extraRegex="請輸入正確的日期格式" data-rule-extraRegex="([12]\d{3}\-(0[1-9]|1[0-2])\-(0[1-9]|[12]\d|3[01]))" />
                                            <a class="btn btn-outline-danger dateClear" href="javascript:void(0);"><i class="fas fa-eraser"></i></a>
                                        </div>
                                    </div>
                                    <label class="col-12 col-md-3 col-lg-2 col-form-label" for="txtInactiveDate@(itemName)">失效日</label>
                                    <div class="col-12 col-md-9 col-lg-4">
                                        <div class="btn-group"  style="width:100%;">
                                            <input type="text" class="form-control" id="txtInactiveDate@(itemName)" name="txtInactiveDate@(itemName)"
                                                   data-msg-extraRegex="請輸入正確的日期格式" data-rule-extraRegex="([12]\d{3}\-(0[1-9]|1[0-2])\-(0[1-9]|[12]\d|3[01]))" />
                                            <a class="btn btn-outline-danger dateClear" href="javascript:void(0);"><i class="fas fa-eraser"></i></a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </fieldset>
                </form>
            </div>
            <div class="card-footer text-center text-lg-right">
                <button type="button" class="btn btn-secondary" onclick="Return@(itemName)()"><i class="fas fa-undo"></i>返回</button>
                <button type="button" class="btn btn-success" onclick="Save@(itemName)()"><i class="fas fa-save"></i>儲存</button>
            </div>
        </div>
    </div>
</div>



@Html.Resource("js",
    @<script>
        let objPage@(itemName) = {
            "pageIndex": 0,
            "pageSize": 10
        };
        let objForm@(itemName) = "#editForm@(itemName)";

        $(document).ready(function () {
            //搜尋日期區間條件
            let currentDate = new Date();
            //單據日期
            //Suites.DateDropper("#txtEffectiveDateQ@(itemName),#txtInactiveDateQ@(itemName)");
            //Suites.SetDateDropper("#txtEffectiveDateQ@(itemName)", currentDate);
            //Suites.SetDateDropper("#txtInactiveDateQ@(itemName)", currentDate);
            Suites.DatePicker("txtEffectiveDateQ@(itemName)", currentDate);
            Suites.DatePicker("txtInactiveDateQ@(itemName)", currentDate);

            $(".dateClear").on("click", function () {
                $(this).siblings("input").val("")
            })
        });

        function Expand@(itemName)() {
            if (parseInt($("#ItemSegmentId").val()) > 0) {
                $(".advanced-block").slideDown("slow");
                Return@(itemName)();
            } else {
                alert("編碼節段資料錯誤", "alert");
            }
        }

        function Query@(itemName)() {
            objPage@(itemName).pageIndex = 0;
            InitData@(itemName)(objPage@(itemName));

            $("#modalQuery@(itemName)").modal("hide");
        }

        function InitData@(itemName)(objPage) {
            let innerHtml = '';
            let pageCount = 0;

            let statusList = [];
            $.each($("input[name='cbxStatusQ@(itemName)']:checked"), function () {
                statusList.push($(this).val());
            });


            let targetUrl = "@Url.Action("GetItemSegmentValue", "PdmBasicInformation")";
            let postData = {
                "OrderBy": "",
                "PageIndex": (objPage.pageIndex + 1),
                "PageSize": objPage.pageSize,
                "ItemSegmentId": $("#ItemSegmentId").val(),
                "SegmentValueNo": $("#txtSegmentValueNoQ@(itemName)").val(),
                "StartDate": ($("#cbxEffectiveDateQ@(itemName)").is(":checked") ? $("#txtEffectiveDateQ@(itemName)").val() : ""),
                "EndDate": ($("#cbxInactiveDateQ@(itemName)").is(":checked") ? $("#txtInactiveDateQ@(itemName)").val() : ""),
                "Status": statusList.join(",")

            };

            let data = DataAccess.Get(targetUrl, postData, false);

            if (data.status == "success") {
                pageCount = data.result.length > 0 ? parseInt(data.result[0]["TotalCount"]) : 0;

                if (pageCount > 0) {
                    objPage = PageCaculate(pageCount, objPage);

                    $.each(data.result, function (i, item) {
                        let _row = (objPage.pageSize * objPage.pageIndex + (i + 1)) + ".";

                        innerHtml += `<tr>
                                        <td>${_row}</td>
                                        <td class="ellipsis" data-toggle="tooltip" title="${item.SegmentValueNo}" style="max-width: 150px;">${item.SegmentValueNo}</td>
                                        <td class="ellipsis" data-toggle="tooltip" title="${item.SegmentValue}" style="max-width: 150px;">${item.SegmentValue}</td>
                                        <td class="text-center auth-status-switch">
                                          <input type="checkbox" name="chkStatus@(itemName)" data-segmentvalue-id="${item.SegmentValueId}" ${item.Status == `A` ? `checked` : ``} />
                                        </td>
                                        <td class="text-center un-auth auth-status-switch">${StatusName(`Status`, item.Status)}</td>
                                        <td class="text-center active-content">
                                          <a class="active-link auth-update" href="javascript:Edit@(itemName)(${item.SegmentValueId});"><i class="fas fa-edit"></i></a>
                                        </td>
                                      </tr>`;
                    });
                } else {
                    innerHtml += `<tr>
                                    <td class="text-center" colspan="5" style="background-color: #fff3cd;">
                                      <i class="fal fa-exclamation-circle"></i>&nbsp;目前尚無資料。
                                    </td>
                                  </tr>`;
                }
            } else {
                alert(data.msg, "alert", 0);
            }

            $("#tblData@(itemName) tbody").html(innerHtml);
            TableComponentInit();
            Pager("#page@(itemName)", pageCount, objPage, InitData@(itemName));

            $("input[name='chkStatus@(itemName)']").bootstrapToggle({
                on: "啟用",
                off: "停用",
                onstyle: "success",
                offstyle: "danger",
                size: "mini"
            });

            $("input[name='chkStatus@(itemName)'][data-segmentvalue-id]").change(StatusSwitch@(itemName));
        }

        function Edit@(itemName)(SegmentValueId) {
            $("#SegmentValueId").val(SegmentValueId ? SegmentValueId : 0);

            Switch("#editData@(itemName)", "#listData@(itemName)");

            $("#txtSegmentValueNo@(itemName)").attr("readonly", false);

            //日期欄位 Suites.DateDropper(功能開啟) Suites.SetDateDropper(日期設置)
            //Suites.DateDropper("#txtEffectiveDate@(itemName), #txtInactiveDate@(itemName)");
            Suites.DatePicker("txtEffectiveDate@(itemName)", "");
            Suites.DatePicker("txtInactiveDate@(itemName)", "");
            //開單日預設今日
            let currentDate = new Date();
            //Suites.SetDateDropper("#txtEffectiveDate@(itemName), #txtInactiveDate@(itemName)", currentDate);

            if (SegmentValueId > 0) {
                $("#txtSegmentValueNo@(itemName)").attr("readonly", true);


                let targetUrl = "@Url.Action("GetItemSegmentValue", "PdmBasicInformation")";
                let postData = {
                    "SegmentValueId": SegmentValueId
                };

                let data = DataAccess.Get(targetUrl, postData, false);

                if (data.status == "success") {
                    $.each(data.result, function (i, item) {
                        $("#txtSegmentValueNo@(itemName)").val(item.SegmentValueNo);
                        $("#txtSegmentValue@(itemName)").val(item.SegmentValue);
                        $("#txtSegmentValueDesc@(itemName)").val(item.SegmentValueDesc);

                        if (item.EffectiveDate != "") {
                            Suites.SetDateDropper("#txtEffectiveDate@(itemName)", new Date(item.EffectiveDate));
                        }
                        if (item.InactiveDate != "") {
                            Suites.SetDateDropper("#txtInactiveDate@(itemName)", new Date(item.InactiveDate));
                        }

                    });
                } else {
                    alert(data.msg, "alert", 0);
                }
            }
        }

        function Save@(itemName)() {
            if ($(objForm@(itemName)).valid()) {
                let SegmentValueId = parseInt($("#SegmentValueId").val());

                if (SegmentValueId <= 0) {
                    let targetUrl = "@Url.Action("AddSegmentValue", "PdmBasicInformation")";
                    let postData = {
                        "ItemSegmentId": $("#ItemSegmentId").val(),
                        "SegmentValueNo": $("#txtSegmentValueNo@(itemName)").val(),
                        "SegmentValue": $("#txtSegmentValue@(itemName)").val(),
                        "SegmentValueDesc": $("#txtSegmentValueDesc@(itemName)").val(),
                        "EffectiveDate": $("#txtEffectiveDate@(itemName)").val(),
                        "InactiveDate": $("#txtInactiveDate@(itemName)").val()
                    };

                    let result = DataAccess.Add(targetUrl, postData, false, true);

                    if (result) {
                        Return@(itemName)();
                    }
                } else {
                    let targetUrl = "@Url.Action("UpdateSegmentValue", "PdmBasicInformation")";
                    let postData = {
                        "ItemSegmentId": $("#ItemSegmentId").val(),
                        "SegmentValueId": $("#SegmentValueId").val(),
                        "SegmentValue": $("#txtSegmentValue@(itemName)").val(),
                        "SegmentValueDesc": $("#txtSegmentValueDesc@(itemName)").val(),
                        "EffectiveDate": $("#txtEffectiveDate@(itemName)").val(),
                        "InactiveDate": $("#txtInactiveDate@(itemName)").val()
                    };

                    let result = DataAccess.Update(targetUrl, postData, false, true);

                    if (result) {
                        Return@(itemName)();
                    }
                }
            }
        }

        

        function Delete@(itemName)(SegmentValueId) {
            swal.fire({
                title: "確認要刪除嗎?",
                text: "此動作無法復原，請確認是否要執行!",
                icon: "warning",
                allowOutsideClick: false,
                allowEscapeKey: false,
                showCancelButton: true,
                confirmButtonColor: '#d33',
                reverseButtons: true
            }).then((result) => {
                if (result.isConfirmed) {
                    let targetUrl = "@Url.Action("DeleteSegmentValue", "PdmBasicInformation")";
                    let postData = {
                        "ItemSegmentId": $("#ItemSegmentId").val(),
                        "SegmentValueId": SegmentValueId
                    };

                    let result = DataAccess.Delete(targetUrl, postData, false, true);

                    if (result) {
                        Query@(itemName)();
                    }
                }
            });
        }

        function StatusSwitch@(itemName)() {
            var SegmentvalueId = $(this).data("segmentvalue-id");

            let targetUrl = "@Url.Action("UpdateItemSegmentValueStatus", "PdmBasicInformation")";
            let postData = {
                "ItemSegmentId": $("#ItemSegmentId").val(),
                "SegmentvalueId": SegmentvalueId
            };

            let result = DataAccess.Update(targetUrl, postData, false, true);

            if (result) {
                Return@(itemName)();
            }
        }

        function Check(str) {
            var temp = "";
            for (var i = 0; i < str.length; i++) {
                if (str.charCodeAt(i) > 0 && str.charCodeAt(i) < 255) {
                    temp += str.charAt(i)
                }
            }
            return temp
        }


        function Return@(itemName)() {
            ResetForm(objForm@(itemName));
            Switch("#listData@(itemName)", "#editData@(itemName)");
            Query@(itemName)();
        }
    </script>
)