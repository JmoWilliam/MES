@using Business_Manager.Helpers
@{
    string itemName = "HistoryPrice";
    string itemNameText = "歷史價格查詢";
    string authority = ViewBag.authority;

    ViewBag.Title = itemNameText;

    string MtlItemNo = Request["MtlItemNo"] ?? "";
}

@Html.Resource("css",
    @<style>
         iframe {
             width: 100%;
             height: 100vh;
         }
    </style>
                )

<iframe id="HistoryPrice@(itemName)"
        src="@Url.Action("IFrameHistoryPrice", "PurchaseRequisition", new { MtlItemNo = MtlItemNo })"
        frameborder="0"
        scrolling="no"></iframe>

@Html.Resource("js",
    @<script>

    </script>
)