@using Business_Manager.Helpers

@Html.Resource("js",
    @<script>
        $(document).ready(function () {
            let generatedURL = "@Url.Action("Index", "MesReport/SaleRankingManagement/search?Company=4")";
            let decodedURL = decodeURIComponent(generatedURL);
            location.replace(decodedURL);

            //location.replace("@Url.Action("Index", "MesReport/SaleRankingManagement/search?Company=4")");
        });
    </script>
)