@using Business_Manager.Helpers
@{
    string itemName = "BarcodeTracking";
    string itemNameText = "條碼追蹤查詢";
    string authority = ViewBag.authority;

    ViewBag.Title = itemNameText;
}

@Html.Resource("css",
    @<style>
         iframe {
             width: 100%;
             height: 100vh;
         }
    </style>
)

<iframe id="ProductionHistory@(itemName)" src="@Url.Action("IFrameBarcodeTracking", "MesReport")" frameborder="0" scrolling="no"></iframe>