@{
    string itemNameText = "揀貨系統";

    ViewBag.Title = itemNameText;
}

<div class="body-bg">
    <div class="wrapper wrap-index">
        <div class="content-index">
            <div class="logo">
                <img src="@Url.Content("~/Content/images/logo.png")" />
            </div>
            <div class="pick-menu">
                <a href="@Url.Action("DeliveryList", "PickingSystem")" class="nav-link">
                    <i class="fas fa-layer-group"></i>所有出貨單
                </a>
                <a href="@Url.Action("CartonList", "PickingSystem")" class="nav-link nav-sub">
                    <i class="fas fa-box-alt"></i>物流箱管理
                </a>
                <a href="javascript:Logout();" class="nav-link nav-cancel">
                    <i class="fas fa-sign-out-alt"></i>登出
                </a>
            </div>
        </div>
    </div>
</div>