@using Business_Manager.Helpers
@{
    string itemName = "ViewAddNgGroup";
    string itemNameText = "新增異常群組";
}

@Html.Resource("css",
    @<style>
    </style>
        )

<div class="modal fade" id="modal@(itemName)">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">新增-不良現象群組</h5>
                <button class="close" data-dismiss="modal" type="button">
                    <span aria-hidden="true"><i class="fas fa-times"></i></span>
                </button>
            </div>
            <div class="modal-body">
                <form class="container" autocomplete="off" id="queryForm@(itemName)">
                    <fieldset>
                        <div class="form-group row">
                            <label class="col-12 col-form-label" for="txtNgGroupNo@(itemName)">群組代碼</label>
                            <div class="col-12">
                                <input type="text" class="form-control" id="txtNgGroupNo@(itemName)" name="txtNgGroupNo@(itemName)" placeholder="請輸入群組代碼"
                                       data-msg-required="請輸入群組代碼" data-rule-required="true"
                                       data-msg-maxlength="必須少於 100 個字元" maxlength="100" />
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-12 col-form-label" for="txtNgGroupName@(itemName)">群組名稱</label>
                            <div class="col-12">
                                <input type="text" class="form-control" id="txtNgGroupName@(itemName)" name="txtNgGroupName@(itemName)" placeholder="請輸入群組名稱"
                                       data-msg-required="請輸入群組名稱" data-rule-required="true"
                                       data-msg-maxlength="必須少於 100 個字元" maxlength="100" />
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-12 col-form-label">狀態</label>
                            <div class="col-12">
                                <select class="form-control" id="cboStatusNo@(itemName)" name="cboStatusNo@(itemName)" aria-invalid="false"></select>
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-12 col-form-label" for="txtNgGroupDesc@(itemName)">描述</label>
                            <div class="col-12">
                                <input type="text" class="form-control" id="txtNgGroupDesc@(itemName)" name="txtNgGroupDesc@(itemName)" placeholder="請輸入影響原因"
                                       data-msg-required="請輸入群組描述" data-rule-required="true"
                                       data-msg-maxlength="必須少於 100 個字元" maxlength="100" />
                            </div>
                        </div>
                    </fieldset>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-success" onclick="Save@(itemName)()"><i class="fas fa-save"></i>儲存</button>
            </div>
        </div>
    </div>
</div>

@Html.Resource("js",
    @<script>
        function Pop@(itemName)() {
            $("#modal@(itemName)").modal({
                backdrop: "static",
                keyboard: false,
                show: true
            });

            ComboBoxs.Default("#cboStatusNo@(itemName)", "@Url.Action("GetStatus", "BasicInformation")", { "StatusSchema": "Status" }, "", "CHOOSE", "", "StatusDesc", "StatusNo");

            if (!isMobile) {
                $("#cboStatusNo@(itemName)").select2({
                    width: "100%"
                });
            }
        }

        function Save@(itemName)() {
            let targetUrl = "@Url.Action("AddNgGroup", "QmsBasicInformation")";
            let postData = {
                "GroupNo": $("#txtNgGroupNo@(itemName)").val(),
                "GroupName": $("#txtNgGroupName@(itemName)").val(),
                "Status": $("#cboStatusNo@(itemName)").val(),
                "GroupDesc": $("#txtNgGroupDesc@(itemName)").val()
            };

            let data = DataAccess.EditContinued(targetUrl, postData, false);
            
            if (data) {
                let id = "NgGroup" + data.result[0]["GroupId"].toString();
                closeModal@(itemName)(id);
                ComboBoxs.Default("#cboNgGroupNoViewAddNgClass", "@Url.Action("GetNgGroup", "QmsBasicInformation")", {}, "", "CHOOSE", "", "NgGroupWithText", "GroupId");
                ComboBoxs.Default("#cboNgGroupNoViewAddNgCause", "@Url.Action("GetNgGroup", "QmsBasicInformation")", {}, "", "CHOOSE", "", "NgGroupWithText", "GroupId");
                ComboBoxs.Default("#cboNgClassNoViewAddNgCause", "@Url.Action("GetNgClass", "QmsBasicInformation")", { "GroupId": $("#cboNgGroupNo@(itemName)").val(), "Status": "A" }, "", "CHOOSE", "", "NgClassWithText", "ClassId");
            }
        }

        function closeModal@(itemName)(id) {
            $("#modal@(itemName)").modal("hide");
            $("#modal@(itemName)").find("input").val("");
            ReturnDefectManagement(id);
        }
    </script>
    )